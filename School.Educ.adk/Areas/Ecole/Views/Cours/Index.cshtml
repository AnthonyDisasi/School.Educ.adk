@model IEnumerable<School.Educ.adk.Areas.Ecole.Models.Cours>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-area="Ecole" asp-controller="Cours" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Intituler)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Classe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Intituler)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categorie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Classe.ID)
            </td>
            <td>
                <a asp-area="Ecole" asp-controller="Cours" asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-area="Ecole" asp-controller="Cours" asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-area="Ecole" asp-controller="Cours" asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
